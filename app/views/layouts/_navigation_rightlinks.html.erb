      <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.lname %>, <%= current_user.fname %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="/">Evalutions</a></li>
            <li><a href="/reporter/index">Reports</a></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          </ul>
        </li>
        <% if current_user.role == "admin" %>
          <li><i class="fa fa-sign-out"></i></li>
          <li><%= link_to "Statistics", stats_surveys_path  %></li>
        <% end %>
      <% else %>
				<li><i class="fa fa-sign-in"></i></li>
        <li><%= link_to "Login", new_user_session_path %></li>
      
      <% end %>